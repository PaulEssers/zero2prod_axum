// This file exists so that api is seen as its own crate, containing all tests.
// default behaviour is that every file under tests is a crate.
mod confirm;
mod healthcheck;
mod subscribe;
mod test_utils;
